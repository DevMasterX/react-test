import { styled } from "styled-components";

const TypographyHeader = styled;

const Header = () => {
  // const isOnline = true;
  return (
    <div>
      <h1>Hello React</h1>
    </div>
  );
};

export default Header;
